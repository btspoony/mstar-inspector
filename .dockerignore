# Keep the sandbox-image Docker build context (repo root) lean.
# The Dockerfile (sandbox-image/omp/Dockerfile) only needs: package.json,
# bun.lock, src/review/, and sandbox-image/verify-synthesis.sh. There is no
# baked models.yml (plan 37). Everything heavy is excluded.

node_modules
.git
.wrangler
.mstar
tests
scripts
*.md
.env*
migrations
.github
wrangler.jsonc
wrangler.smoke.jsonc
tsconfig.json
LICENSE
README.md
