/** Header the SPA `postForm` client sends so dashboard POST handlers can distinguish fetch from native navigation. */
export const SPA_POST_FORM_HEADER = "X-Mstar-Spa-Post";
export const SPA_POST_FORM_VALUE = "1";
